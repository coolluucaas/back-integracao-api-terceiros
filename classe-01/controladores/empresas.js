const instanciaAxios = require('../servicos/company_enrichment')

const obterInfoEmpresa = async (req, res) => {
    try {
        const infoPromise
        
    } catch (error) {
        console.log(error)
    }
}

module.exports = {
    obterInfoEmpresa
}
